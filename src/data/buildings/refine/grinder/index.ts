import type { BuildItem } from '../../types'

const grinder: BuildItem = {
    id: 'grinder',
    name: 'grinder',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default grinder
