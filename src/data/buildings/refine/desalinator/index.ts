import type { BuildItem } from '../../types'
import image from './desalinator.png'

const desalinator: BuildItem = {
    id: 'desalinator',
    name: 'desalinator',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default desalinator
