import type { BuildItem } from '../../types'

const emulsifier: BuildItem = {
    id: 'emulsifier',
    name: 'emulsifier',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default emulsifier
