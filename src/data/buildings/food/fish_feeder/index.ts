import type { BuildItem } from '../../types'
import image from './fish_feeder.png'

const fish_feeder: BuildItem = {
    id: 'fish_feeder',
    name: 'fish_feeder',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default fish_feeder
