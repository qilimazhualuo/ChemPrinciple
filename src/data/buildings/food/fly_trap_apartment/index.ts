import type { BuildItem } from '../../types'
import image from './fly_trap_apartment.png'

const fly_trap_apartment: BuildItem = {
    id: 'fly_trap_apartment',
    name: 'fly_trap_apartment',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'building',
}

export default fly_trap_apartment
