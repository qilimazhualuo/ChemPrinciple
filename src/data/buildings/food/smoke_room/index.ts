import type { BuildItem } from '../../types'

const smoke_room: BuildItem = {
    id: 'smoke_room',
    name: 'smoke_room',
    icon: '▣',
    image: '',
    width: 3,
    height: 2,
    layer: 'building',
}

export default smoke_room
