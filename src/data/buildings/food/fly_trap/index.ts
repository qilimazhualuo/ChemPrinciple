import type { BuildItem } from '../../types'

const fly_trap: BuildItem = {
    id: 'fly_trap',
    name: 'fly_trap',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default fly_trap
