import type { BuildItem } from '../../types'
import image from './spice_grinder.png'

const spice_grinder: BuildItem = {
    id: 'spice_grinder',
    name: 'spice_grinder',
    icon: '▣',
    image: image,
    width: 2,
    height: 2,
    layer: 'building',
}

export default spice_grinder
