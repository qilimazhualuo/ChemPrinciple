import type { BuildItem } from '../../types'
import image from './snow_tile.png'

const snow_tile: BuildItem = {
    id: 'snow_tile',
    name: 'snow_tile',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default snow_tile
