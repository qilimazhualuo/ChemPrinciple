import type { BuildItem } from '../../types'
import image from './wicker_door.png'

const wicker_door: BuildItem = {
    id: 'wicker_door',
    name: 'wicker_door',
    icon: '▣',
    image: image,
    width: 1,
    height: 2,
    layer: 'building',
}

export default wicker_door
