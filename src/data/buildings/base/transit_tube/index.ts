import type { BuildItem } from '../../types'

const transit_tube: BuildItem = {
    id: 'transit_tube',
    name: 'transit_tube',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'road',
}

export default transit_tube
