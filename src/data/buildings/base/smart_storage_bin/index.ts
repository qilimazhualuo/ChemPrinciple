import type { BuildItem } from '../../types'
import image from './smart_storage_bin.png'

const smart_storage_bin: BuildItem = {
    id: 'smart_storage_bin',
    name: 'smart_storage_bin',
    icon: '▣',
    image: image,
    width: 1,
    height: 2,
    layer: 'building',
}

export default smart_storage_bin
