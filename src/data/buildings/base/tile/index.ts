import type { BuildItem } from '../../types'
import image from './tile.png'

const tile: BuildItem = {
    id: 'tile',
    name: 'tile',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default tile
