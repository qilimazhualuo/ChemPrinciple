import type { BuildItem } from '../../types'

const plant_analyzer: BuildItem = {
    id: 'plant_analyzer',
    name: 'plant_analyzer',
    icon: '▣',
    image: '',
    width: 4,
    height: 3,
    layer: 'building',
}

export default plant_analyzer
