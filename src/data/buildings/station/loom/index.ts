import type { BuildItem } from '../../types'

const loom: BuildItem = {
    id: 'loom',
    name: 'loom',
    icon: '▣',
    image: '',
    width: 4,
    height: 3,
    layer: 'building',
}

export default loom
