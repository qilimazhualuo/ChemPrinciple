import type { BuildItem } from '../../types'
import image from './super_computer.png'

const super_computer: BuildItem = {
    id: 'super_computer',
    name: 'super_computer',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default super_computer
