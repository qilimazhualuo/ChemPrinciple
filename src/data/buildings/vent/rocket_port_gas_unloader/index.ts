import type { BuildItem } from '../../types'

const rocket_port_gas_unloader: BuildItem = {
    id: 'rocket_port_gas_unloader',
    name: 'rocket_port_gas_unloader',
    icon: '▣',
    image: '',
    width: 2,
    height: 3,
    layer: 'building',
}

export default rocket_port_gas_unloader
