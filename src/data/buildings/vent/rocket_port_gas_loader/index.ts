import type { BuildItem } from '../../types'
import image from './rocket_port_gas_loader.png'

const rocket_port_gas_loader: BuildItem = {
    id: 'rocket_port_gas_loader',
    name: 'rocket_port_gas_loader',
    desc: '将气体装载到火箭的港口',
    icon: '▣',
    image: image,
    width: 2,
    height: 3,
    layer: 'building',
}

export default rocket_port_gas_loader
