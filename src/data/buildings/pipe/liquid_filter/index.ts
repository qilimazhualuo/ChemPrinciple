import type { BuildItem } from '../../types'

const liquid_filter: BuildItem = {
    id: 'liquid_filter',
    name: 'liquid_filter',
    icon: '▣',
    image: '',
    width: 3,
    height: 1,
    layer: 'liquid_pipe',
}

export default liquid_filter
