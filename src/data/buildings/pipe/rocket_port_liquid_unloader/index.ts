import type { BuildItem } from '../../types'
import image from './rocket_port_liquid_unloader.png'

const rocket_port_liquid_unloader: BuildItem = {
    id: 'rocket_port_liquid_unloader',
    name: 'rocket_port_liquid_unloader',
    desc: '从火箭卸载液体的港口',
    icon: '▣',
    image: image,
    width: 2,
    height: 3,
    layer: 'building',
}

export default rocket_port_liquid_unloader
