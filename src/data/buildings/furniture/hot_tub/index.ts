import type { BuildItem } from '../../types'
import image from './hot_tub.png'

const hot_tub: BuildItem = {
    id: 'hot_tub',
    name: 'hot_tub',
    icon: '▣',
    image: image,
    width: 5,
    height: 2,
    layer: 'building',
}

export default hot_tub
