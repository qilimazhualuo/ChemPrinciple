import type { BuildItem } from '../../types'
import image from './aero_pot.png'

const aero_pot: BuildItem = {
    id: 'aero_pot',
    name: 'aero_pot',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default aero_pot
