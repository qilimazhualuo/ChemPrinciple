import type { BuildItem } from '../../types'
import image from './bed.png'

const bed: BuildItem = {
    id: 'bed',
    name: 'bed',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default bed
