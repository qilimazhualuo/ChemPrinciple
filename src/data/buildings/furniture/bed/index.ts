import type { BuildItem } from '../../types'
import image from './bed.png'

const bed: BuildItem = {
    id: 'bed',
    name: 'bed',
    icon: '▣',
    image: image,
    width: 2,
    height: 2,
    layer: 'building',
}

export default bed
