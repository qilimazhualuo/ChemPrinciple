import type { BuildItem } from '../../types'

const canvas_wide: BuildItem = {
    id: 'canvas_wide',
    name: 'canvas_wide',
    icon: '▣',
    image: '',
    width: 3,
    height: 2,
    layer: 'background',
}

export default canvas_wide
