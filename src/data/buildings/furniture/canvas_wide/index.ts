import type { BuildItem } from '../../types'

const canvas_wide: BuildItem = {
    id: 'canvas_wide',
    name: 'canvas_wide',
    desc: '宽幅画布，可绘制更大的画作',
    icon: '▣',
    image: '',
    width: 3,
    height: 2,
    layer: 'background',
}

export default canvas_wide
