import type { BuildItem } from '../../types'

const space_scanner: BuildItem = {
    id: 'space_scanner',
    name: 'space_scanner',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default space_scanner
