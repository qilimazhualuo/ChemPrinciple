import type { BuildItem } from '../../types'
import image from './signal_broadcaster.png'

const signal_broadcaster: BuildItem = {
    id: 'signal_broadcaster',
    name: 'signal_broadcaster',
    icon: '▣',
    image: image,
    width: 1,
    height: 2,
    layer: 'automation',
}

export default signal_broadcaster
