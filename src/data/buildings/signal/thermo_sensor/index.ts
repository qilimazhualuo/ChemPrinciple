import type { BuildItem } from '../../types'

const thermo_sensor: BuildItem = {
    id: 'thermo_sensor',
    name: 'thermo_sensor',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'automation',
}

export default thermo_sensor
