import type { BuildItem } from '../../types'

const switch_signal: BuildItem = {
    id: 'switch_signal',
    name: 'switch_signal',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'automation',
}

export default switch_signal
