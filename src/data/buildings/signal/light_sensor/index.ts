import type { BuildItem } from '../../types'

const light_sensor: BuildItem = {
    id: 'light_sensor',
    name: 'light_sensor',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'automation',
}

export default light_sensor
