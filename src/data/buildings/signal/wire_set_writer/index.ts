import type { BuildItem } from '../../types'

const wire_set_writer: BuildItem = {
    id: 'wire_set_writer',
    name: 'wire_set_writer',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'automation',
}

export default wire_set_writer
