import type { BuildItem } from '../../types'
import image from './logic_filter.png'

const logic_filter: BuildItem = {
    id: 'logic_filter',
    name: 'logic_filter',
    desc: '过滤信号脉冲的逻辑过滤门',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'automation',
}

export default logic_filter
