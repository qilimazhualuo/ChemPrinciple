import type { BuildItem } from '../../types'
import image from './hydro_sensor.png'

const hydro_sensor: BuildItem = {
    id: 'hydro_sensor',
    name: 'hydro_sensor',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'automation',
}

export default hydro_sensor
