import type { BuildItem } from '../../types'

const logic_not: BuildItem = {
    id: 'logic_not',
    name: 'logic_not',
    desc: '将信号取反的逻辑非门',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'automation',
}

export default logic_not
