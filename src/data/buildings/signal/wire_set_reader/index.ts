import type { BuildItem } from '../../types'
import image from './wire_set_reader.png'

const wire_set_reader: BuildItem = {
    id: 'wire_set_reader',
    name: 'wire_set_reader',
    desc: '从自动化线缆组读取数据',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'automation',
}

export default wire_set_reader
