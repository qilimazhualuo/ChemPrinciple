import type { BuildItem } from '../../types'

const logic_splitter: BuildItem = {
    id: 'logic_splitter',
    name: 'logic_splitter',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default logic_splitter
