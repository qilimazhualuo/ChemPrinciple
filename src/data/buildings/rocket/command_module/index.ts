import type { BuildItem } from '../../types'
import image from './command_module.png'

const command_module: BuildItem = {
    id: 'command_module',
    name: 'command_module',
    icon: '▣',
    image: image,
    width: 5,
    height: 5,
    layer: 'building',
}

export default command_module
