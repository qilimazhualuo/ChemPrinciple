import type { BuildItem } from '../../types'

const command_capsule: BuildItem = {
    id: 'command_capsule',
    name: 'command_capsule',
    icon: '▣',
    image: '',
    width: 5,
    height: 5,
    layer: 'building',
}

export default command_capsule
