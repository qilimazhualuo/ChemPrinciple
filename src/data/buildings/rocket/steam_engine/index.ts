import type { BuildItem } from '../../types'

const steam_engine: BuildItem = {
    id: 'steam_engine',
    name: 'steam_engine',
    icon: '▣',
    image: '',
    width: 5,
    height: 3,
    layer: 'building',
}

export default steam_engine
