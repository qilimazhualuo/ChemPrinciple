import type { BuildItem } from '../../types'

const biofuel_engine: BuildItem = {
    id: 'biofuel_engine',
    name: 'biofuel_engine',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default biofuel_engine
