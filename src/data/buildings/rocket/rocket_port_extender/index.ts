import type { BuildItem } from '../../types'
import image from './rocket_port_extender.png'

const rocket_port_extender: BuildItem = {
    id: 'rocket_port_extender',
    name: 'rocket_port_extender',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default rocket_port_extender
