import type { BuildItem } from '../../types'
import image from './ladder_attachment.png'

const ladder_attachment: BuildItem = {
    id: 'ladder_attachment',
    name: 'ladder_attachment',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'building',
}

export default ladder_attachment
