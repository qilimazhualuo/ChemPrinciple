import type { BuildItem } from '../../types'
import image from './liquid_hydrogen_engine.png'

const liquid_hydrogen_engine: BuildItem = {
    id: 'liquid_hydrogen_engine',
    name: 'liquid_hydrogen_engine',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default liquid_hydrogen_engine
