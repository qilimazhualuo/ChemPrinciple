import type { BuildItem } from '../../types'

const cargo_tank: BuildItem = {
    id: 'cargo_tank',
    name: 'cargo_tank',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default cargo_tank
