import type { BuildItem } from '../../types'
import image from './bio_cargo_tank.png'

const bio_cargo_tank: BuildItem = {
    id: 'bio_cargo_tank',
    name: 'bio_cargo_tank',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
}

export default bio_cargo_tank
