import type { BuildItem } from '../../types'

const launch_pad: BuildItem = {
    id: 'launch_pad',
    name: 'launch_pad',
    icon: '▣',
    image: '',
    width: 7,
    height: 2,
    layer: 'building',
}

export default launch_pad
