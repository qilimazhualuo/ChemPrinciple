import type { BuildItem } from '../../types'

const hand_sanitizer: BuildItem = {
    id: 'hand_sanitizer',
    name: 'hand_sanitizer',
    icon: '▣',
    image: '',
    width: 2,
    height: 3,
    layer: 'building',
}

export default hand_sanitizer
