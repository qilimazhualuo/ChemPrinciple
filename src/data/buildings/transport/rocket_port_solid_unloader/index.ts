import type { BuildItem } from '../../types'

const rocket_port_solid_unloader: BuildItem = {
    id: 'rocket_port_solid_unloader',
    name: 'rocket_port_solid_unloader',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default rocket_port_solid_unloader
