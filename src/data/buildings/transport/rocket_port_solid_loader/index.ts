import type { BuildItem } from '../../types'
import image from './rocket_port_solid_loader.png'

const rocket_port_solid_loader: BuildItem = {
    id: 'rocket_port_solid_loader',
    name: 'rocket_port_solid_loader',
    icon: '▣',
    image: image,
    width: 2,
    height: 3,
    layer: 'building',
}

export default rocket_port_solid_loader
