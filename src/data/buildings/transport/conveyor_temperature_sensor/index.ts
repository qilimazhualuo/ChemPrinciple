import type { BuildItem } from '../../types'

const conveyor_temperature_sensor: BuildItem = {
    id: 'conveyor_temperature_sensor',
    name: 'conveyor_temperature_sensor',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'automation',
}

export default conveyor_temperature_sensor
