import type { BuildItem } from '../../types'
import image from './air_filter.png'

const air_filter: BuildItem = {
    id: 'air_filter',
    name: 'air_filter',
    icon: '▣',
    image: image,
    width: 1,
    height: 2,
    layer: 'building',
}

export default air_filter
