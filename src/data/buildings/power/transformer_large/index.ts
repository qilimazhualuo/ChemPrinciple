import type { BuildItem } from '../../types'

const transformer_large: BuildItem = {
    id: 'transformer_large',
    name: 'transformer_large',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default transformer_large
