import type { BuildItem } from '../../types'

const smart_battery: BuildItem = {
    id: 'smart_battery',
    name: 'smart_battery',
    icon: '▣',
    image: '',
    width: 2,
    height: 2,
    layer: 'building',
}

export default smart_battery
