import type { BuildItem } from '../../types'

const wire: BuildItem = {
    id: 'wire',
    name: 'wire',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'wire',
}

export default wire
