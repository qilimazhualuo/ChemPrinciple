import type { BuildItem } from '../../types'
import image from './wire_refined_bridge.png'

const wire_refined_bridge: BuildItem = {
    id: 'wire_refined_bridge',
    name: 'wire_refined_bridge',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'wire',
}

export default wire_refined_bridge
