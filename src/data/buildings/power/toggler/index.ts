import type { BuildItem } from '../../types'

const toggler: BuildItem = {
    id: 'toggler',
    name: 'toggler',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
    layer: 'building',
}

export default toggler
