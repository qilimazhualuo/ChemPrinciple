import type { BuildItem } from '../../types'

const power_plug: BuildItem = {
    id: 'power_plug',
    name: 'power_plug',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default power_plug
