import type { BuildItem } from '../../types'
import image from './manual_generator.png'

const manual_generator: BuildItem = {
    id: 'manual_generator',
    name: 'manual_generator',
    icon: '▣',
    image: image,
    width: 4,
    height: 2,
    layer: 'building',
}

export default manual_generator
