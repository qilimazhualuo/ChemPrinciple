import type { BuildItem } from '../../types'

const dev_generator: BuildItem = {
    id: 'dev_generator',
    name: 'dev_generator',
    icon: '▣',
    image: '',
    width: 1,
    height: 1,
}

export default dev_generator
