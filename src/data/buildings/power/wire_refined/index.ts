import type { BuildItem } from '../../types'
import image from './wire_refined.png'

const wire_refined: BuildItem = {
    id: 'wire_refined',
    name: 'wire_refined',
    desc: '由精炼金属制成的导电线',
    icon: '▣',
    image: image,
    width: 1,
    height: 1,
    layer: 'wire',
}

export default wire_refined
